name: example
description: Flutter biometric authentication and secure MQTT IoT bridge. Verify users with platform biometrics, generate cryptographic tokens, and trigger trusted device actions with hardware-backed security.


environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  biometric_iot_bridge:
    path: ../

dev_dependencies:
  flutter_lints: ^3.0.0
