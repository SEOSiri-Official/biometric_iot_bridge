name: biometric_iot_bridge_example
description: Example app demonstrating Flutter biometric authentication and secure MQTT IoT bridge — verify users with platform biometrics, generate cryptographic tokens, and trigger trusted device actions.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  biometric_iot_bridge:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0